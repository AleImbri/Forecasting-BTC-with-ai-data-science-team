STRICT RULES FOR H2O REGRESSION:
1. NO CONNECTION CHECKS: NEVER use 'h2o.is_running', 'h2o.cluster()', or 'ice_core'. 
2. H2O INIT: Simply use 'h2o.init()' at the start. It handles connections automatically.
3. PROBLEM TYPE: This is a REGRESSION task. Target 'y_mfe_log' is numeric.
4. NO CLASSIFICATION: NEVER use 'asfactor()' on the target.
5. H2O SYNTAX: 
   - aml = H2OAutoML(max_runtime_secs={max_runtime_secs}, seed=1, stopping_metric='RMSE')
   - aml.train(y=target_name, training_frame=data_h2o)
6. ROBUSTNESS: 
   - Check 'if aml.leaderboard is not None and aml.leaderboard.nrows > 0' before saving.
7. IMPORTS: Include 'import h2o', 'from h2o.automl import H2OAutoML', and 'import pandas as pd'.